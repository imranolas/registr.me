= form_for(resource, :as => resource_name, :url => user_registration_path, class: 'navbar-form', remote: true) do |f|
  %div
    = f.label :email, "Invite a user"
    %br/
    = f.email_field :email, :autofocus => true
  %div= f.submit "Sign up", class: 'btn btn-primary'
