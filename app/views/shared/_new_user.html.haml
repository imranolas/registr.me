= form_for(resource, :as => resource_name, :url => registration_path(resource_name), class: 'navbar-form') do |f|
  %div
    = f.label :email
    %br/
    = f.email_field :email, :autofocus => true
  %div= f.submit "Sign up"
